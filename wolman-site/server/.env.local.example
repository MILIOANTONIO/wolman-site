# Copia questo file in ".env.local" (stesso cartella) e incolla la tua chiave.
# Il file ".env.local" NON viene mai letto da me, non va su git, e resta solo sul tuo computer.
#
# Dove trovare/creare una chiave: https://console.anthropic.com/settings/keys
#
ANTHROPIC_API_KEY=
